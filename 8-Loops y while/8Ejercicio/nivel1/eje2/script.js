// Número máximo de clientes
let maxClientes = 20;

// Asignar mesas a los clientes
for (let cliente = 1; cliente <= maxClientes; cliente++) {
    let mesa = cliente;
    alert(`¡Bienvenid@ a mi café! Su mesa es la ${mesa}.`);
}